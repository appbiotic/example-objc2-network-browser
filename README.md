# example-objc2-network-browser

Example output:

```
Browsing with service_type `_http._tcp` domain: ``
Browsing...
---- Browser Event, change: ADDED
New: service `9x25 MobileAP MDNS` domain `local.` expect more `false`
---- Browser Event, change: ADDED
New: service `zeroconf_example_service` domain `local.` expect more `false`
---- Browser Event, change: REMOVED
Old: service `zeroconf_example_service` domain `local.` expect more `false`

^CShutting down
```